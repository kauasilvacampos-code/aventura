constavanca=docunent.querySelectorAll(.btn-proximo)
avancaForEach(button {
   buttonaddEventListener('click',function(){
     constatual=document.querySelector('ativo');
     constproximoPasso='passo-' tthis.getAttrbute('data-proximo');
     atual-classList.remove('ativo')
     document.getElementByTd(proximopasso).classList.add('ativo');
   })
})
